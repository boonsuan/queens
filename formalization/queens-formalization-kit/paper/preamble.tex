% !TeX root = queens.tex
\usepackage[letterpaper,margin=1in]{geometry}
\usepackage[T1]{fontenc}
\usepackage{lmodern,amssymb,mathtools,microtype,booktabs,capt-of,placeins,needspace,etoolbox}
\usepackage{xcolor}
\usepackage{algorithm,algpseudocode}
% amsart leaves 12pt after each caption, expecting captions above tables.
% Captions here sit below their figures and tables, so drop that space.
\setlength{\belowcaptionskip}{0pt}
% Leave spare space at the foot of a page rather than between paragraphs.
\raggedbottom
\usepackage{tikz,chessfss}
\usetikzlibrary{decorations.pathreplacing}
% latexmk enables caching; ordinary editor builds can render TikZ directly.
\ifnum\pdfshellescape=1
  \IfFileExists{build/tikz-settings.tex}{\input{build/tikz-settings}}{}
\fi
\definecolor{queensUpper}{HTML}{087E8B}
\definecolor{queensLower}{HTML}{C66A2B}
\usepackage{hyperref} % \href
\definecolor{bluey}{rgb}{0,0,0.6}
\hypersetup{
  pdftitle={Greedy queens and the golden ratio},
  pdfauthor={Boon Suan Ho},
  bookmarksnumbered=true,
  bookmarksopen=true,
  bookmarksdepth=2,
  pdfpagemode=UseOutlines,
  colorlinks,
  linkcolor=bluey,
  citecolor=bluey,
  urlcolor=bluey
}

% Our table captions follow the tables; amsart supplies no gap above them.
% Apply the same spacing to floats and to \captionof{table}.
\makeatletter
\AddToHook{cmd/@makecaption/before}{%
  \ifdefstring{\@captype}{table}{\addvspace{8pt}}{}%
}
\makeatother

% https://tex.stackexchange.com/questions/319398/
% making-the-dot-in-the-theorem-normal-but-not-as-like-bf
\makeatletter
\def\thmhead@plain#1#2#3{%
  \thmname{#1}\thmnumber{\@ifnotempty{#1}{ }\@upn{#2}}%
  \thmnote{ \the\thm@notefont(#3)}}% no group inside \thmnote
\let\thmhead\thmhead@plain
\makeatother

\newtheorem{theorem}{Theorem}
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{corollary}[theorem]{Corollary}
\theoremstyle{definition}
\newtheorem{definition}[theorem]{Definition}
% End definitions with a corner, using the same placement as a proof's QED.
\AddToHook{env/definition/begin}{%
  \renewcommand{\qedsymbol}{\ensuremath{\lrcorner}}%
  \pushQED{\qed}}
\AddToHook{env/definition/end}{\popQED}
\newtheorem{condition}[theorem]{Condition}
\theoremstyle{remark}
\newtheorem*{remark}{Remark}
% Give remarks the same closing corner as definitions.
\AddToHook{env/remark/begin}{%
  \renewcommand{\qedsymbol}{\ensuremath{\lrcorner}}%
  \pushQED{\qed}}
\AddToHook{env/remark/end}{\popQED}
\theoremstyle{plain}
\newcommand{\N}{\mathbb N}
% Knuth's range dots: http://ftp.cs.stanford.edu/tex/local/lib/taocpmac.tex
\newcommand{\dts}{\mathinner{\ldotp\ldotp}}
\newcommand{\J}{\mathcal J}
\newcommand{\last}{\operatorname{last}_{12}}
\newcommand{\todo}[1]{\textcolor{red!60}{#1}}
% Source-only notes; discard the argument without expanding it.
\newcommand{\xnote}[1]{}
\allowdisplaybreaks[1]
\emergencystretch=1em
\title[Greedy queens and the golden ratio]{Greedy queens and the golden ratio}
\author{Boon Suan Ho}
\address{Department of Mathematics, National University of Singapore}
\email{hbs@u.nus.edu}
\date{}
